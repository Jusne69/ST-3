#GUI_canvas2
from tkinter import *
class window_one:
    def __init__(self):
        Button(main_window,text="Quit",command=self.quit) .grid()
        Button(main_window.text="Oval",command=self.oval) .grid()
        Button(main_window,text="Rectngle",command=self.rectangle) .grid()
      